package edu.byohttp;

import edu.byohttp.RequestImpl;
import edu.byohttp.Request;

import java.io.IOException;


import org.junit.Test;
import static org.junit.Assert.*;

public class RequestTest {

    @Test public void testRequest() throws IOException {
    /*    String method = "GET";
        String resource = "/hellowworld.html";
        String version = "HTTP/1.1A";

        Request request = new RequestImpl();
       // request.setMethod(method);
      //  request.setVersion(version);
      //  request.setResource(resource);
        assertEquals("GET", request.getMethod());
        assertEquals("/hellowworld.html", request.getResource());
        assertEquals("HTTP/1.1A", request.getVersion());

     */

    }
}